Tasks:
	- Cache downloaded images to disk during viewing session. Possibly use SDWebImage (https://github.com/rs/SDWebImage)
	- Move to a delegate pattern instead of passing MWPhoto objects before presenting view.
Changes:
